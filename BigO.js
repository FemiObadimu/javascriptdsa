//Understanding Time Complexity and Space Complexity;
// three greek letter omega(best case) , theta(average case) , omicron(worst case)

function logItems(n) {
  for (let i = 0; i < n; i++) {
    console.log(i);
  }
}
logItems(10);

//This Code is O(n) which is proportional in a graph

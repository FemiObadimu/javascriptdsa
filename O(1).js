function addingItems(n) {
  return n + n;
}

addingItems(2);

//The code above is O(1) because there is only one operation.

function addingItemes(n) {
  return n + n + n;
}

addingItemes(2);
//The code above is O(2) because there are two operation.
